# Python dependencies for CANdata2matcsv

# Core dependencies
pandas>=1.3.0
numpy>=1.21.0
scipy>=1.7.0

# CAN tools
cantools>=36.0.0
python-can>=4.0.0

# GUI (usually included with Python, but listing for completeness)
# tkinter is included in standard Python distribution

# Note: If you encounter issues with tkinter on Linux, you may need to install it via:
# Ubuntu/Debian: sudo apt-get install python3-tk
# Fedora: sudo dnf install python3-tkinter
# macOS: tkinter comes with Python from python.org
